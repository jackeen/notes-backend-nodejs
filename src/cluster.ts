// import os from "os";
// import cluster from "cluster";

// const cpus = os.cpus();

// if (cluster.isPrimary) {
// 	cpus.forEach((cpu) => {
// 		cluster.fork();
// 	});

// }

// if (cluster.isWorker) {

// }